import { createSlice } from "@reduxjs/toolkit";

export const initialState = {};

export const notefolioSlice = createSlice({
  name: "todoList",
  initialState,
  reducers: {},
});
export const {} = notefolioSlice.actions;
export default notefolioSlice.reducer;
